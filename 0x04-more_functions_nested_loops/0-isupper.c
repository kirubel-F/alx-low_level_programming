#include "main.h"
/**
* _isupper - checks for uppercase character
* @c: Variable text
* Return: Always 0.
*/

int _isupper(int c)
{
  char c;
  
  if (c >= 'A' && c <= 'z')
    return (1);
}
  else if {(c>='a' && c<='z'){
    
    return (0);
  }  
  
}
